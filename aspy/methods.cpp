#include <cstdio>
using namespace std;

 gauss_seidel(Y, V0, S, tipos, eps)
{
    double delta;
    V;
    do
    {
        for (int i = 0; i < N; i++)
        {
            
            V[i] = ()/Y[i][i]
        }
    } while (delta > eps);
    
}

void main ()
{
    return;
}
